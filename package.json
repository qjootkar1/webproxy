{
  "name": "swiftproxy",
  "version": "1.0.0",
  "description": "Free anonymous web proxy",
  "private": true,
  "engines": {
    "node": ">=20.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  }
}
